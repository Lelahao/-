# 排座助手 · 桌面版（Electron + Python + SQLite）验收清单

> **用途**：Windows 端到端手工验收；每条勾选「通过」并记录日期/版本号。  
> **相关文档**：`README.md`、`docs/排座助手_桌面版交付说明.md`。  
> **默认后端**：`http://127.0.0.1:8765`，健康检查 `GET /api/health`。

---

## 0. 验收前准备

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 0.1 | 记录构建版本 | `package.json` version、Git commit、installer 文件名一致 | □ | |
| 0.2 | 准备干净虚拟机或新用户配置 | 无旧版排座助手用户数据（或已知路径可清空） | □ | 便于复现「首次安装」 |
| 0.3 | 准备工具 | 任务管理器、浏览器或 `curl`、资源管理器 | □ | |

---

## 1. Windows 干净环境安装

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 1.1 | 运行 `dist:win` 产出的 NSIS 安装包 | 安装向导完成，无致命错误 | □ | 若缺 `build/icons/icon.ico` 可能导致图标/构建异常，见交付说明 |
| 1.2 | 勾选自定义目录（若向导提供） | 可选路径安装成功，快捷方式可启动 | □ | `package.json` 中 `allowToChangeInstallationDirectory: true` |
| 1.3 | 安装完成后磁盘布局 | 程序目录含应用与 resources；`extraResources` 内含 `paizuo-backend.exe` | □ | 可与交付说明对照 |

---

## 2. 首次启动

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 2.1 | 从开始菜单/桌面启动应用 | 主窗口打开，无白屏崩溃 | □ | 生产包加载 `dist/index.html` |
| 2.2 | 开发模式（可选）`npm run electron:dev` | Vite `1420` 就绪后 Electron 加载页面；后端子进程由主进程拉起 | □ | 与安装包路径不同 |

---

## 3. 后端自动启动

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 3.1 | 启动应用后等待数秒 | `GET http://127.0.0.1:8765/api/health` 返回 200 | □ | 可与浏览器或 curl 验证 |
| 3.2 | 任务管理器 | 存在 `paizuo-backend.exe`（或开发态下 `python` + uvicorn） | □ | 端口可被 `PAIZUO_BACKEND_PORT` / `PAIZUO_PORT` 覆盖，默认 8765 |
| 3.3 | 主进程日志 | `electron/logger.ts` 写入 `%userData%/logs/electron-YYYY-MM-DD.log` 含 `backend healthy` 一类记录 | □ | 路径见下文「数据与日志」 |

---

## 4. SQLite 数据目录创建

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 4.1 | 首次成功启动后检查用户目录 | 存在 `…/userData/paizuo-data/`（或其下 `paizuo.db`） | □ | Electron 设置 `PAIZUO_DATA_DIR` 指向该目录 |
| 4.2 | 开发态仅跑 `npm run dev:backend`（不经 Electron） | 数据落在仓库 `server/data`（未设置环境变量时） | □ | 与桌面数据目录可能不一致，属预期 |
| 4.3 | 数据库可读写 | 后续「人员/桌位」操作无 SQLite 锁错误 | □ | |

---

## 5. 方案 CRUD

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 5.1 | 在「方案管理」页：新建 / 编辑名称 / 删除方案，并比对 SQLite | 列表与 `plans` 表一致；操作后刷新列表正确 | □ | **`PlansPage` 已对接 `listPlans`、`getPlanDetail`、`createPlan`、`updatePlan`、`deletePlan`**（桌数、人数、分布来自各方案详情） |
| 5.2 | 或用 REST 直接验收：`POST/GET/PATCH/DELETE /api/plans...` | 符合 `server` 路由与 OpenAPI/实现 | □ | 适合绕过 UI 做接口级验收 |

---

## 6. 人员维护

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 6.1 | 在已选方案下增删改人员 | `dbApi` → `people` API → SQLite 持久化 | □ | 依赖方案 ID 与后端可用 |
| 6.2 | 刷新或重启应用 | 人员数据仍在 | □ | |

---

## 7. 桌位维护

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 7.1 | 保存桌形、席位 | `tables` / `seats` API 成功，库内记录更新 | □ | |
| 7.2 | 非法数据（如空方案） | 合理错误提示或 4xx，应用不崩溃 | □ | 按当前实现 |

---

## 8. 左侧导航折叠持久化

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 8.1 | 切换侧栏折叠状态 | UI 立即变化 | □ | |
| 8.2 | 完全退出并重启 | 折叠状态与退出前一致 | □ | 经 `dbApi.saveUISetting` / `GET/PATCH /api/settings/...`，key 与历史 Tauri 一致时可对照 `docs/tauri_to_rest_api_mapping.md` |

---

## 9. 全屏模式展示人名

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 9.1 | 进入圆桌全屏总览 | 桌、席、人名可视化正确 | □ | |
| 9.2 | 分辨率/缩放 | 无截断错乱（抽查 100%/125%） | □ | |

---

## 10. 全屏拖拽保存

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 10.1 | 拖拽调整席位标签/关联人 | 交互流畅 | □ | |
| 10.2 | 保存布局后刷新或重进 | 布局从 `layout` API 或回退存储恢复 | □ | 见 `src/fullscreen/roundStorage.ts` 与 `src/api/layouts.ts` |

---

## 11. 导出功能

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 11.1 | 导出 JSON / Excel / Word / PPT / 图片 / SVG | 本机生成下载文件，可打开 | □ | `src/lib/planExport.ts`：浏览器端生成，**不依赖外网** |
| 11.2 | 后端 `export_routes`（若产品启用） | 与前端流程一致且无重复冲突 | □ | 按需 |

---

## 12. 离线使用

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 12.1 | 断开公网，保留本机 127.0.0.1 | 应用与后端可用，数据读写正常 | □ | 无云端依赖场景 |
| 12.2 | 导出、全屏 | 仍可用（导出为本地库） | □ | |

---

## 13. 应用关闭后后端进程退出

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 13.1 | 正常关闭所有窗口退出 | 任务管理器中 `paizuo-backend.exe` 结束（开发态为对应 python 子树） | □ | `before-quit` → `stopBackend()`；Windows 使用 `taskkill /T /F` |
| 13.2 | 异常路径（可选） | 无大量孤儿进程累积 | □ | |

---

## 14. 日志可查

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 14.1 | 打开 `userData/logs/electron-*.log` | 含主进程、`backend-out`/`backend-err` 等行 | □ | |
| 14.2 | 后端标准输出 | 开发态子进程日志进入上述文件或控制台（视运行方式） | □ | |

**常见路径提示（Windows）**：Electron `userData` 多为 `%APPDATA%\排座助手`（与 `productName` 等相关），以 `app.getPath('userData')` 为准；其下 **`paizuo-data/paizuo.db`**（生产环境桌面 SQLite）、**`logs/electron-*.log`**。卸载默认**不删**用户数据，除非另行设计清理策略；详见 `docs/排座助手_桌面版交付说明.md` §2。

**关键词验收说明**：

- **`electron/preload.ts` 中的 `ipcRenderer.invoke("paizuo:getEnv")`**：Electron IPC，**不属于** Tauri `@tauri-apps/api` 残留。
- **`dist-electron/preload.js`**：由 `npm run electron:compile` 生成，已 `.gitignore`，**不作为**源码中 `invoke(` 残留判断依据。

---

## 15. 卸载 / 重装数据策略

| # | 步骤 | 预期 | 通过 □ | 备注 |
|---|------|------|--------|------|
| 15.1 | 卸载应用（控制面板/设置） | 程序文件移除；**用户数据是否删除取决于安装器选项与 NSIS 配置** | □ | 多数场景 `userData` 仍保留 → **重装后数据可能仍在** |
| 15.2 | 文档化团队策略 | 明确：升级保留数据路径；彻底清理需手动删 `userData` | □ | 避免误删生产数据 |
| 15.3 | 重装后启动 | 若未删 `userData`，应读到原 `paizuo.db` | □ | |

---

## 16. 与本仓库静态配置对照（供验收填表参考）

| 项 | 位置 | 说明 |
|----|------|------|
| 前端 API 基址 | `src/api/client.ts` | 默认 `http://127.0.0.1:8765`，`VITE_API_BASE_URL` 可覆盖 |
| 开发后端脚本 | `package.json` → `dev:backend` | `uvicorn` 绑定 `127.0.0.1:8765` |
| Electron 端口 | `electron/paths.ts` | `PAIZUO_BACKEND_PORT` / `PAIZUO_PORT`，默认 8765 |
| 健康检查 | `electron/backendProcess.ts` | `GET /api/health` |
| 数据目录 | `electron/paths.ts` + `server/core/config.py` | `PAIZUO_DATA_DIR`；否则 Python 侧为 `server/data` |
| 打包后端 | `package.json` build.extraResources | `dist-backend/paizuo-backend.exe` |

**已修复（阶段 9）**：`PlansPage` 已从后端加载方案并支持新建 / 编辑 / 删除；统计卡片中「总方案数」为实时数量，其余仪表盘指标仍为占位（待接入活动/导出统计）。**阶段 10（方案管理交互补齐）验收条目见下文 §17；阶段 12（方案版本管理）见 §18。**

---

## 17. 阶段 10：方案管理页交互补齐（验收）

> 与上文 **「## 10. 全屏拖拽保存」** 编号不同：本节为**产品阶段 10**（方案管理页），请勿混淆。

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| 10.1 | **新建方案** · 桌次人数配置 | 可填方案名称/备注；可配置桌别、桌型、桌数、每桌人数；可新增/删除一类桌次；预计总人数自动计算；提交后创建 `tables` 与占位 `people`；列表卡片桌数=`tables.length`、人数=`people.length` | □ | `PlansPage` 新建弹窗 + `createPlan` / `saveTables` / `putPeople` |
| 10.2 | **编辑方案** · 桌次人数配置 | 可改名称、备注、桌次配置；可增/减桌数与人数（减操作有确认）；保存后刷新列表；整页刷新后数据仍正确 | □ | `getPlanDetail` → `updatePlan` / `saveTables` / `putPeople` |
| 10.3 | **统计数据来源** | 列表展示的桌数来自 **`detail.tables.length`**，人数来自 **`detail.people.length`**；**不在** `plans` 表新增 `table_count` / `guest_count` 等冗余统计字段 | □ | 见 `toViewPlan`（`PlansPage.tsx`） |
| 10.4 | **发布方案** | `draft` 可发布；非草稿不重复提供可用「发布」入口；状态筛选可选到 `published` 等 | □ | 更多菜单/编辑内发布确认弹窗 |
| 10.5 | **导入与模板** | 可下载 **Excel** 导入模板（`.xlsx`）；可导入合法 **Excel**（`.xlsx`/`.xls`）；非法文件有校验提示；导入后列表刷新，桌数/人数与详情一致 | □ | **非 JSON 文件导入**；解析见 `PlansPage` / `parsePlanImportExcel` |
| 10.6 | **搜索 / 筛选 / 视图** | 可按方案 `name`、`note` 搜索；可清空恢复；状态筛选含全部/草稿/已发布/已完成/已归档；可在 **card** / **compact** 间切换 | □ | 前端过滤 + `PlanRow.status` 归一筛选 |
| 10.7 | **更多菜单（···）** | 含：编辑方案、发布方案（仅 draft 可点）、**导出 Excel**（多工作表：方案/桌次/人员/座位）、复制方案（待接入提示）、删除方案（确认弹窗）；点击菜单外关闭 | □ | 导出为 `.xlsx`，时间列已格式化为可读字符串 |
| 10.8 | **桌次管理** | 顶部按钮有响应：弹窗说明 + 方案下拉，**进入管理**跳转 `/round/overview`（带 `state`）；非静默无操作 | □ | 圆桌总览与方案数据后续可对齐，属后续迭代 |
| 10.9 | **最近动态** | 侧栏仍为 **静态** 占位列表 | □ | 后续可接 `activity_logs` / 后端动态 |

**与本阶段一致的工程约束（代码审阅勾选）**：

| # | 检查项 | 预期 |
|---|--------|------|
| C.1 | `src` 中无 `@tauri-apps/api`、无 Tauri `invoke` | 当前为 Electron + 本地 REST |
| C.2 | `package.json` 无 `@tauri-apps/*`、`tauri` CLI 依赖 | 与上一致 |
| C.3 | 构建产物目录未纳入版本库 | `.gitignore` 含 `dist/`、`dist-electron/`、`dist-backend/`、`release/`、`server/build/`、`server/dist/`、`logs/` 等 |

**说明**：阶段 11–14 文档编号与产品阶段编号不同；**产品阶段 12（方案版本管理）** 的逐条验收见 **§18**。

---

## 18. 阶段 12（产品）：方案版本管理（验收）

> **入口**：圆桌排座总览 `/round/overview`，须从「方案管理」进入并写入关联 `planId`（`localStorage`：`paizuo-round-linked-plan-id`），且本地后端可用。  
> **相关代码**：`TopToolbar.tsx`（保存版本）、`RoundVersionHistoryDrawer.tsx`（列表 / 查看 / 导出版本弹窗）、`src/api/plans.ts`、`server/repositories/plan_version_repo.py`、`plan_versions.snapshot_json`。

### 18.1 保存版本

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| 12.S1 | 排座后保存 | 在总览完成排座调整后，点击顶栏 **「保存版本」**（圆桌总览；其他页顶栏为「保存方案」指本机布局快照，与阶段 12 **后端版本** 不同） | □ | |
| 12.S2 | 保存版本弹窗 | 弹出可填写 **版本名称**、**版本备注** 的对话框；名称非空方可提交 | □ | `TopToolbar` |
| 12.S3 | 持久化 | 提交后：本机 `saveLayoutSnapshot`、**推送当前排座至后端** `pushRoundPlanToBackend`，再 **`POST /api/plans/{planId}/versions`** 创建记录 | □ | 写入 `plan_versions` |
| 12.S4 | 多次保存 | 多次保存产生递增 **版本号**（`version_no`），列表中可见 **V1 / V2 / …** | □ | |
| 12.S5 | 快照不随当前方案漂移 | **已保存行的 `snapshot_json`** 为保存时刻完整快照；之后在总览继续改排座 **不会改写** 历史版本行 | □ | 仅新保存产生新行 |

### 18.2 版本列表

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| 12.L1 | 打开列表 | 顶栏 **「版本记录」** 打开抽屉，**`GET .../versions`** 列出该方案全部版本 | □ | |
| 12.L2 | 字段展示 | 每项含：**版本号**、**名称**、**备注**、**保存时间**、**桌数 / 人数 / 已安排 / 未安排** | □ | `PlanVersionListItem` |
| 12.L3 | 排序 | 后端 **`ORDER BY version_no DESC, created_at DESC`**（新版本号在前；版本号单调递增时与「新保存优先」一致） | □ | `plan_version_repo.list_plan_versions` |
| 12.L4 | 数量 | 多次保存后，列表条数与库内 `plan_versions` 行数一致 | □ | SQLite 可抽查 |

### 18.3 版本查看

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| 12.V1 | 查看入口 | 每条版本有 **「查看」**，请求 **`GET .../versions/{versionId}`** | □ | |
| 12.V2 | 快照内容 | 预览展示 **该次保存时** `snapshot_json` 解析后的排座总览，而非当前内存编辑态 | □ | `getPlanVersion` → `snapshotToLayoutSnapshot` |
| 12.V3 | 只读 | 预览层内 **不可拖拽、不可改座** | □ | 桌卡不传 drop / reorder 处理函数 |
| 12.V4 | 隔离当前方案 | **不写入** `useRoundPlanDemoStore`；关闭预览后当前方案 **未被覆盖** | □ | |
| 12.V5 | 关闭恢复 | 关闭预览回到总览/抽屉，继续编辑当前方案 | □ | |

### 18.4 版本导出

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| 12.E1 | 导出入口 | 每条版本 **「导出」** → **「导出版本」** 弹窗，可选 PNG / XLSX / DOCX / PPTX | □ | |
| 12.E2 | 数据来源 | 再次 **`getPlanVersion`**，仅用 **`snapshot_json`** 构建 `ExportScene`；**不得**用当前总览或仅 `getPlanDetail` 代替历史座次数据 | □ | `exportPlanVersionSnapshot` |
| 12.E3 | 格式结果 | 四种格式均可下载；未实现格式须 **`alert`** 提示「该格式导出功能待接入」（当前四种已接） | □ | `planExport.ts` |
| 12.E4 | 差异校验 | **V1 与 V2** 排座不同时，同格式导出文件内容应 **可区分** | □ | |

### 18.5 `snapshot_json` 说明

| 项 | 说明 |
|----|------|
| 存储位置 | SQLite **`plan_versions.snapshot_json`**（`TEXT`，JSON） |
| 写入时机 | 用户保存版本：服务端 **`build_plan_snapshot`** 组装 **当时** 的方案概要、**`people` / `tables` / `seats`** 及圆桌 **`layout`**、统计等并序列化入库 |
| 前端消费 | **`getPlanVersion`** → `snapshot` → **`snapshotToLayoutSnapshot`** → 总览预览与导出 |
| 不变性 | 修改当前方案或布局 **不回写** 已存在版本行；仅 **新建版本** 增加新行 |

### 18.6 已知限制

| # | 限制 | 说明 |
|---|------|------|
| L.1 | 导出标题/文件名 | 与总览传入的 **`planDisplayName`**（当前与默认方案展示名一致）绑定；若需与 **`plans.name`** 完全一致，可在进入总览时注入后端名称 |
| L.2 | 列表排序主键 | 以 **`version_no` DESC** 为主；若存在数据迁移导致的序号异常，需单独治理 |
| L.3 | 依赖后端与关联 | 未关联后端 `planId` 或后端未启动时，保存 / 列表 / 查看 / 导出将不可用或提示错误 |

### 18.7 阶段 12 工程审阅（与 §17 并列）

| # | 检查项 | 预期 |
|---|--------|------|
| C.4 | 未重新引入 Tauri | 与 §17 C.1–C.2 一致 |
| C.5 | **`plans` 表无** `table_count` / `guest_count` **等冗余统计列** | 统计在 `plan_versions` 行字段或运行时计算；**`plans` 仍为** `id, name, note, status, created_at, updated_at` |
| C.6 | 构建产物未入库 | 与 §17 C.3 一致 |

---

## 19. 阶段 13.3：人员添加、人员管理与 Excel 导入（验收）

> **范围**：圆桌排座总览 `/round/overview`、全屏总览、人员管理弹窗、批量导入与导入结果弹窗。  
> **关联代码**：`AddPersonModal.tsx`、`PersonManageModal.tsx`、`BulkImportPeopleModal.tsx`、`ImportResultModal.tsx`、`RoundOverviewPage.tsx`、`FullscreenRoundOverview.tsx`、`src/api/plans.ts`、`server/api/people_routes.py`、`server/repositories/people_repo.py`。

### 19.1 人员添加验收

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| A.1 | 打开弹窗 | 总览未安排卡片「+ 添加人员」、人员管理「+ 添加人员」、全屏未安排区「+ 添加人员」均可打开 **添加人员 · 当前方案** | □ | `z-[110]` |
| A.2 | 字段 | 含 **姓名、区域、岗位、角色** | □ | |
| A.3 | 必填 | 四项 `trim` 后均非空方可提交 | □ | 否则 `alert` 提示 |
| A.4–A.7 | **角色为自定义** | **角色**为 **`<input type="text">`**；**无** `<select>` / datalist；**无**默认值与预置选项 | □ | 与席位方位「主陪/主宾」配置无关 |
| A.8 | 未安排 | 新增人员入 **`people`** 且无座位分配，总览 **未安排** 列表可见 | □ | 后端 `create_person` |
| A.9 | 统计刷新 | **总人数** `plan.people.length`、**未安排人数**随 `refreshPlanFromBackend` 更新 | □ | 不依赖 `plans` 表冗余人数列 |

### 19.2 人员字段与数据检查

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| D.1 | SQLite `people` | 持久化 **`region` / `position` / `role`**（及 `display_name`） | □ | `people_repo.create_person` / `update_person` |
| D.2 | `role` 无默认/枚举 | 库内可为任意非空字符串；**无** DB CHECK 枚举 | □ | |
| D.3 | `GET /api/plans/{id}` | `PlanDetail.people[]` 含展示所需四字段映射：`displayName`、`region`、`position`、`role`、分配字段 | □ | `PersonRow` |
| D.4 | 删除人员与座位 | **`delete_person`** 先 **`clear_seats_for_person`** 再删人，**seats 不残留**指向已删人员的 `person_id` | □ | `people_repo.delete_person` |

### 19.3 人员管理验收

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| M.1–M.2 | 页签 | **未安排（N）** / **已安排（N）**，列表字段含 **姓名、区域、岗位、角色、状态、操作** | □ | |
| M.3–M.7 | 搜索 | 可对 **姓名、区域、岗位、角色** 及 **桌号、座位号** 子串筛选（同一搜索框） | □ | `PersonManageModal` |
| M.8 | 编辑 | 打开 **编辑人员**（复用添加弹窗，`updatePerson`） | □ | |
| M.9 | 删除 | 未安排 / 已安排均有确认；已安排删除提示含桌号座号 | □ | |
| M.10 | 退回 | **退回未安排** 调 **`POST .../unassign`**，清座位、人员保留 | □ | |

### 19.4 Excel 模板验收

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| T.1 | 下载 | 批量导入弹窗内 **下载 Excel 模板**，文件为 **`.xlsx`** | □ | 文件名：`{方案展示名}_人员导入模板.xlsx` |
| T.2–T.3 | 工作表 | **Sheet1：`人员导入`**；**Sheet2：`填写说明`** | □ | `downloadPeopleImportTemplateFile` |
| T.4 | 表头 | **人员导入** 首行为 **姓名、区域、岗位、角色**（可无数据行，避免角色「示例默认值」） | □ | |
| T.5–T.7 | 角色列 | **无**下拉、**无**默认值、**无**数据验证（DV）；纯单元格 | □ | SheetJS `aoa_to_sheet` |
| T.8 | 说明文案 | **填写说明** 中含 **角色为自定义 / 自由文本**，与产品约束一致 | □ | |

### 19.5 Excel 批量导入验收

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| B.1–B.2 | 选择/拖拽 | 支持 **选择** 与 **拖拽** 单个 `.xlsx`；多文件拖拽提示 | □ | |
| B.3–B.5 | 前端校验 | 非 xlsx、缺表头、空/无可导入行等有 **明确提示** | □ | `validatePeopleImportFile` |
| B.6 | 合法导入 | 通过后 **`POST .../people/import`**，追加写入 | □ | 不覆盖、不删已有人员 |
| B.9 | 未安排 | 成功导入人员 **`assigned_table_id` / `assigned_seat_no` 为空** | □ | `import_people_rows` |
| B.10–B.11 | 排座与版本 | **不**自动排座；**不**自动调用 **保存版本** API | □ | 仅 `refreshPlanFromBackend` + 本机快照（与既有总览一致） |

### 19.6 导入结果与失败明细验收

| # | 验收项 | 预期 | 通过 □ | 备注 |
|---|--------|------|--------|------|
| R.1 | 结果弹窗 | 导入返回后关闭批量弹窗，打开 **导入结果 · 当前方案**（`z-[125]`） | □ | 部分成功 / 全成功 / 全失败顶栏文案 |
| R.2 | 成功表 | 列出 **姓名、区域、岗位、角色** | □ | `result.success` |
| R.3 | 失败表 | **姓名、原因**；可 **下载失败明细** | □ | |
| R.4 | 失败明细文件 | **`人员导入失败明细.xlsx`**，列：**行号、姓名、区域、岗位、角色、失败原因** | □ | 无 DV |

### 19.7 与「保存版本」的关系说明

| 项 | 说明 |
|----|------|
| **人员添加 / Excel 导入** | 只更新 **`people`（及活动日志）**，**不**写入 **`plan_versions`**。 |
| **总览刷新** | `refreshPlanFromBackend` 拉 **`getPlanDetail`** 同步内存方案与（若启用）本机布局快照；**不等于**保存历史版本。 |
| **何时产生版本** | 仅在用户主动执行 **顶栏「保存版本」**（`POST /api/plans/{id}/versions`）时落库快照，规则见 **§18**。 |
| **约束重申** | 不重复建设版本表 / 版本 API；**`plans` 表**仍不新增 `table_count` / `guest_count` / `people_count` 等统计列。 |

### 19.8 阶段 13.3 工程审阅（勾选）

| # | 检查项 | 预期 |
|---|--------|------|
| C.7 | 未重新引入 Tauri | 与 §17 C.1 一致 |
| C.8 | 人员/桌数统计 | UI 与逻辑以 **`people.length` / `tables.length`**（或等价 `plan` 快照）为准 |
| C.9 | 版本 API | 仅复用既有 **`/versions`**，不重复实现 |
| C.10 | 不入库构建产物与样例导入文件 | `.gitignore` 含 `dist/`、`release/`、`dist-electron/`、`dist-backend/` 等；**勿提交**临时下载模板/测试 xlsx |

---

## 20. 签核

| 角色 | 姓名 | 日期 | 版本 |
|------|------|------|------|
| 验收人 | | | |
| 复核 | | | |
